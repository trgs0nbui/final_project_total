// Global test setup for Vitest
// Add global mocks or configuration here as needed

// Example: suppress console.warn for known Vue/Element Plus warnings in tests
// vi.spyOn(console, 'warn').mockImplementation(() => {})
